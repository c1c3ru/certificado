import 'package:flutter/material.dart';

class CertificatesGeneratorPage extends StatelessWidget {
  const CertificatesGeneratorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      child: const Column(
        children: [
          // TODO: Implementar o conteúdo da página aqui
          Text('Página de Gerador de Certificados'),
        ],
      ),
    );
  }
}
